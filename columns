import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
        appBar: AppBar(
          title: Text("Clarice Store"),
        ),
        body: Container(
            color: Colors.black,
            child: Center(
                child: Column(
              children: [
                Icon(
                  Icons.home,
                  color: Colors.white,
                ),
                Text("HOME", style: TextStyle(color: Colors.white)),
              ],
            )))),
  ));
}
